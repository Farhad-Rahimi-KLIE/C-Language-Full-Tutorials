#include <stdio.h>
int main()
{
    // Variable -> a name is given to a memory Location
    
    // Basic Data Types -> int, float, char, double;
    // Derived Data Types -> Array, Pointers, Structure, Union;

//  this is const variable
    const int a = 34;
    // a = 45; this way is not good and take a error
    printf("%d", a);
    return 0;
}