#include <stdio.h>
int main()
{
    /*
    
      1 - format specifiers is a way to tell the compuilar what type of data is in a variable
      during taking input displaying output to the user;
      2 - printf("this is a good boy %d") is print the integer in the screen
    */

    // Example of Format Specifiers
    
    // 1 - %c -   for printf of char
    // 2 - %d -  for printf of int
    // 3 - %f -  for printf of float
    // 4 - %l -  for printf of Long
    // 5 - %d -  for printf of double
    return 0;
}