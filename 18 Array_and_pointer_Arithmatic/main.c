#include <stdio.h>
int main()
{
    // if iam ++ the pointer accept the architecture of computer increament
    // Example ->  int integer is require space in the architecture of computer is  bytes
    // char 1 bytes float 4 bytes and double is 4 bytes
    //  so pointer increament the value of own by bytes in computer 

    // Example ->

    // with int variables

    // int a = 30;
    // int* pointer = &a;
    // pointer++;
    // pointer++;
    // printf("pointer value is %d", pointer);

    // with char variables

    // char s = 'f';
    // char* pointer = &s;
    // pointer++;
    // pointer++;
    // printf("pointer value is %d", pointer);

//    Example of using bytes in array
                    [1,   2,   3,   4,   5,   6,   7];
 // index            0    1    2    3    4    5    6
//values by bytes 1000 1004 1008 1012   1016 1020 1024

    return 0;
}